# User guide

go-parts is pre-v1. This guide is populated as user-facing features ship — Phase 1 brings the
daemon, CLI, and REST API.

Planned pages:

- `install.md` — binary / Docker / `docker compose` setup
- `quickstart.md` — first part, first search
- `usage.md` — day-to-day workflows (stock adjust, low-stock, BOM import)
- `cli.md` — the `go-parts` command reference

Until then, see the [PRD](../internals/go-parts-prd.md) for what's being built and the
[architecture overview](../reference/architecture.md) for how it fits together.
